<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=title%>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
    
    <!-- adding Jquery link -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.0/jquery.min.js"></script>
    
    
    <link rel="stylesheet" href="/css/layout.css">
    <!-- Now whenever we put style sheet to sub-pages they automatically come here because we've set the property in app.js that extract stylesheet and script sheet and put it into their original position  -->
    <%- style %>
</head>
<body>
    <%- include('_header.ejs'); -%>
    
    
    <%-body-%>

    <%- include('_footer.ejs'); -%>
    <!-- Now whenever we put style sheet or script to sub-pages they automatically come here because we've set the property in app.js that extract stylesheet and script sheet and put it into their original position  -->
    <%- script %>
    <script>
        <% if(flash.success && flash.success.length > 0) {%>
        new Noty({
            theme: 'relax',
            text: '<%= flash.success%>',
            type: 'success',
            layout: 'topRight',
            timeout: 1500

        }).show();
        <% } %>

        <% if(flash.error && flash.error.length > 0) {%>
        new Noty({
            theme: 'relax',
            text: '<%= flash.error%>',
            type: 'error',
            layout: 'topRight',
            timeout: 1500

        }).show();
        <% } %>
    </script>
</body>
</html>